#ifndef __CERVER_HTTP_PARSER
#define __CERVER_HTTP_PARSER

// Here will be defined a HTTP request parser, to identify items on HTTP headers and so on...

class HttpParser {
    private:
    public:
};

#endif // __CERVER_HTTP_PARSER